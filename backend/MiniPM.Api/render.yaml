services:
  - type: web
    name: mini-pm-api
    env: dotnet
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/MiniPM.Api.dll
    envVars:
      - key: ASPNETCORE_URLS
        value: http://+:10000
